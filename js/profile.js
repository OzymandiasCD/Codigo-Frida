$(document).ready(function () {

    // Materialize tooltip initialization
    $('.small-badge').tooltip({
        position: 'top',
        enterDelay: 0,
        exitDelay: 0,
        margin: 0
    });
    
});