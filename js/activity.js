$(document).ready(function () {

    // Materialize tooltip initialization
    $('.tooltipped').tooltip({
        position: 'top',
        margin: 48,
        enterDelay: 0,
        exitDelay: 0
    });

});