$(document).ready(function () {

    // Materialize tooltip initialization
    $('.tooltipped').tooltip({
        position: 'top',
        margin: 50,
        enterDelay: 0,
        exitDelay: 0
    });

});