$(document).ready(function () {

    // Materialize tab initialization
    $('.tabs').tabs();

    // Materialize tooltip initialization
    $('.tooltipped').tooltip({
        position: 'top'
    });

});